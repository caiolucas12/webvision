{
  "name": "webvision",
  "version": "1.0.0",
  "description": "WebVision - Sistema de Monitoramento de Rede",
  "scripts": {
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "0.17.4"
  }
}
